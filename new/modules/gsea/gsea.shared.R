source(app_path("modules", "gsea", "gsea.config.R"), local = TRUE, encoding = "UTF-8")
source(app_path("modules", "gsea", "gsea.plot.R"), local = TRUE, encoding = "UTF-8")
source(app_path("modules", "gsea", "gsea.export.R"), local = TRUE, encoding = "UTF-8")
source(app_path("modules", "gsea", "gsea.compute.R"), local = TRUE, encoding = "UTF-8")
